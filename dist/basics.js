"use strict";
const addNumbers = (num1, num2, showResults) => {
    if (showResults)
        return num1 + num2;
};
let unknown;
const number1 = 2;
const number2 = 2.8;
const printCheck = true;
const results = addNumbers(number1, number2, printCheck);
console.log("🚀 --- results", results);
